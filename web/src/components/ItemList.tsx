import styled from 'styled-components';

const ListWrapper = styled.div`
    border: 1px solid black;
`

const ItemList = () => {
    return (
        <ListWrapper>
            <h1>Items</h1>
        </ListWrapper>
    )
}

export { ItemList };

